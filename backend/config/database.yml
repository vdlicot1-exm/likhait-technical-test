default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("DATABASE_HOST") { "localhost" } %>
  username: <%= ENV.fetch("DATABASE_USERNAME") { "root" } %>
  password: <%= ENV.fetch("DATABASE_PASSWORD") { "" } %>
  database: <%= ENV.fetch("DATABASE_NAME") { "expense_system_development" } %>

development:
  <<: *default

test:
  <<: *default
  database: expense_system_test
  username: <%= ENV.fetch("TEST_DATABASE_USERNAME") { ENV.fetch("DATABASE_USERNAME") { "root" } } %>
  password: <%= ENV.fetch("TEST_DATABASE_PASSWORD") { ENV.fetch("DATABASE_PASSWORD") { "" } } %>

production:
  <<: *default
